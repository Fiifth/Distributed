TESTEN
1) fileReplicatie
	- algemene replicatie
		1) maakt aantal nodes aan
		2) plaats enkel bestanden in een lokale map van een node
		3) controleer of ze naar juiste nodes gerepliceerd zijn (in replicatie folder aanwezig)
	- lokale file verwijderen
		1) verwijder een file uit de lokale folder van een node
		2) Controleer of file is verwijderd op replicatie node
	- nieuwe replicatie eigenaar
		1) start een nieuwe node op
		2) controleer of bestaande nodes gerepliceerde bestanden overplaatsen naar nieuwe eigenaar indien nodig
		3) controleer of het overgeplaatst bestand verwijderd is bij oude replicatie eigenaar
	- afsluiting node
		1) sluit een node af
		2) controleer of replicatie bestanden overgeplaatst worden naar een nieuwe replicatie eigenaar
		3) controleer of bestanden verwijderd worden waarvan de node lokale eigenaar was
		
2) FileAgent
	- aanvullen netwerk files
		1) maak minstens 2 nodes
		2) zet bestanden in lokale folders van nodes
		3) controleer of bestanden in de netwerkfile map komt te staan
	- downloaden files
	- verwijderen files

3) meerdere eigenaars
	1) maak minstens 2 nodes
	2) geef de nodes hetzelfde lokale bestand
	3) verwijder lokaal bestand op 1 node
	4) controleer of bestand niet verwijderd wordt op replicatie node
	5) verwijder lokaal bestand ook op 2de node
	6) controlleer of bestand wel verwijderd wordt op replicatie node
	

4) nodeOrder
	-start verschillende nodes op
		1) 2 willekeurige nodes
		2) 1 node die op het einde van de cirkel toegevoegd wordt
		3) 1 node die in het begin van de cirkel toegevoegd wordt
		4) 1 node die in het midde toegevoegd wordt
		5) een node toevoegen met een naam die reeds gebruikt is aanmaken
		6) controlleer of de volgorde correct is
	-afsluiten node
		1) sluit een node af
		2) controlleer of de volgorde juist is geupdate
		
		
localOwners -->Map<ID,IP>
